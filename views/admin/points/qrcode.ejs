<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <title>QR 碼</title>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.qrcode/1.0/jquery.qrcode.min.js"></script>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: rgb(0, 46, 93);
            color: white;
            font-family: Arial, sans-serif;
        }
        #qrcode {
            text-align: center;
        }
        img {
            max-width: 300px; /* 限制 QR 碼的最大寬度 */
            height: auto;
        }
    </style>
</head>
<body>

    <div id="qrcode">
        <h1>掃描 QR 碼 拿到點數</h1>
        <div id="qrcodeImage"></div>
    </div>

    <script>
        $(document).ready(function() {
            const qrCodeData = "<%= eventId %>,<%= pointId %>"; // 格式為 eventId,pointId
            $('#qrcodeImage').qrcode({
                text: qrCodeData,
                width: 256,
                height: 256
            });
        });
    </script>

</body>
</html>