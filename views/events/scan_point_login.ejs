<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>掃瞄加分登入</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .login-container {
            background: rgba(255, 255, 255, 0.95);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            max-width: 400px;
            width: 100%;
        }
        .login-title {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
            font-weight: bold;
        }
        .pin-input {
            font-size: 24px;
            text-align: center;
            letter-spacing: 8px;
            font-family: 'Courier New', monospace;
            padding: 15px;
        }
        .login-btn {
            width: 100%;
            padding: 12px;
            font-size: 18px;
            margin-top: 20px;
        }
        #message {
            margin-top: 15px;
        }
    </style>
</head>
<body>

<div class="login-container">
    <h2 class="login-title">掃瞄加分登入</h2>
    <form id="loginForm">
        <div class="form-group">
            <label for="pin">請輸入 6 位數字 PIN 碼</label>
            <input type="text" class="form-control pin-input" id="pin" 
                   placeholder="000000" maxlength="6" pattern="[0-9]{6}" required>
        </div>
        <button type="submit" class="btn btn-primary login-btn">登入</button>
    </form>
    <div id="message"></div>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script>
$(document).ready(function() {
    const eventId = '<%= eventId %>';
    
    // 限制只能輸入數字
    $('#pin').on('input', function() {
        this.value = this.value.replace(/[^0-9]/g, '');
    });
    
    // 自動聚焦
    $('#pin').focus();
    
    // 登入表單提交
    $('#loginForm').on('submit', function(e) {
        e.preventDefault();
        
        const pin = $('#pin').val();
        
        if (pin.length !== 6) {
            $('#message').html('<div class="alert alert-danger">請輸入 6 位數字 PIN 碼</div>');
            return;
        }
        
        $.ajax({
            url: `/events/${eventId}/attendee/login`,
            type: 'POST',
            contentType: 'application/json',
            data: JSON.stringify({ pin: pin }),
            success: function(response) {
                if (response.success) {
                    window.location.href = `/events/${eventId}/attendee/scan`;
                } else {
                    $('#message').html('<div class="alert alert-danger">' + response.message + '</div>');
                }
            },
            error: function(xhr) {
                const errorMsg = xhr.responseJSON?.message || '登入失敗';
                $('#message').html('<div class="alert alert-danger">' + errorMsg + '</div>');
            }
        });
    });
});
</script>

</body>
</html>


