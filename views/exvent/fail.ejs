<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>付款失敗</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .container {
            max-width: 800px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            padding: 30px;
            margin-top: 30px;
            margin-bottom: 30px;
        }
        .form-header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #e9ecef;
        }
        .form-header h1 {
            color: #2c3e50;
            font-weight: 600;
            margin-bottom: 10px;
        }
        .fail-content {
            text-align: center;
            padding: 40px 20px;
        }
        .fail-icon {
            font-size: 64px;
            color: #dc3545;
            margin-bottom: 20px;
        }
        .fail-title {
            color: #2c3e50;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 20px;
        }
        .fail-message {
            color: #6c757d;
            font-size: 1.1rem;
            margin-bottom: 30px;
        }
        .btn-container {
            text-align: center;
            margin-top: 40px;
            padding-top: 30px;
            border-top: 2px solid #e9ecef;
        }
        .btn {
            padding: 12px 30px;
            font-weight: 500;
            border-radius: 5px;
            margin: 0 10px;
            min-width: 120px;
        }
        .btn-secondary {
            background-color: #95a5a6;
            border-color: #95a5a6;
        }
        .btn-secondary:hover {
            background-color: #7f8c8d;
            border-color: #7f8c8d;
        }
        .transaction-details {
            background-color: #f8f9fa;
            border-radius: 5px;
            padding: 20px;
            margin: 20px 0;
            text-align: left;
        }
        .transaction-details h5 {
            color: #2c3e50;
            margin-bottom: 15px;
        }
        .transaction-item {
            padding: 8px 0;
            border-bottom: 1px solid #dee2e6;
        }
        .transaction-item:last-child {
            border-bottom: none;
        }
        .error-msg {
            color: #dc3545;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- 輪播 -->
        <div id="carouselExampleCaptions" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="/exvent/<%= event_id %>.jpg" class="d-block w-100" alt="第一張圖片" 
                         onerror="this.src='/exvent/banner.jpg'">
                    <div class="carousel-caption d-none d-md-block">
                    </div>
                </div>
            </div>
        </div>

        <div class="form-header">
            <div class="fail-icon">✗</div>
            <h1 id="failTitle" data-zh="登記失敗" data-en="Registration Failed">登記失敗</h1>
        </div>

        <div class="fail-content">
            <% if (transaction) { %>
                <h5 id="sorryTitle" data-zh="很抱歉，您的付款未能成功" data-en="Sorry, your payment was not successful">很抱歉，您的付款未能成功</h5>
                <p id="checkMessage" data-zh="請檢查以下付款細節：" data-en="Please check the following payment details:">請檢查以下付款細節：</p>
                
                <div class="transaction-details">
                    <h5 id="transactionTitle" data-zh="交易詳情" data-en="Transaction Details">交易詳情</h5>
                    <div class="transaction-item">
                        <strong id="orderLabel" data-zh="訂單編號：" data-en="Order Number: ">訂單編號：</strong>
                        <span><%= transaction && transaction._id ? transaction._id : '' %></span>
                    </div>
                    <div class="transaction-item">
                        <strong id="ticketLabel" data-zh="票券名稱：" data-en="Ticket Name: ">票券名稱：</strong>
                        <span><%= transaction && transaction.ticketTitle ? transaction.ticketTitle : '' %></span>
                    </div>
                    <div class="transaction-item">
                        <strong id="priceLabel" data-zh="票券價格：" data-en="Ticket Price: ">票券價格：</strong>
                        <span>$<%= transaction && transaction.ticketPrice ? transaction.ticketPrice : '' %></span>
                    </div>
                    <div class="transaction-item">
                        <strong id="statusLabel" data-zh="付款狀態：" data-en="Payment Status: ">付款狀態：</strong>
                        <span><%= transaction && transaction.status ? transaction.status : '' %></span>
                    </div>
                    <% if (errorMsg) { %>
                        <div class="transaction-item">
                            <strong id="errorLabel" data-zh="錯誤訊息：" data-en="Error Message: ">錯誤訊息：</strong>
                            <span class="error-msg"><%= errorMsg %></span>
                        </div>
                    <% } %>
                </div>
            <% } else { %>
                <h5 id="sorryTitle" data-zh="很抱歉，您的登記未能成功" data-en="Sorry, your registration was not successful">很抱歉，您的登記未能成功</h5>
                <p id="retryMessage" data-zh="請稍後再試，或聯絡主辦單位。" data-en="Please try again later, or contact the organizer.">請稍後再試，或聯絡主辦單位。</p>
            <% } %>

            <div class="btn-container">
                <a href="/web/<%= event_id %>/register<%= lang ? '?lang=' + lang : '' %>" class="btn btn-secondary" id="backBtn" data-zh="返回主頁" data-en="Return to Homepage">返回主頁</a>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        // 從 URL 參數獲取語言
        function getLanguageFromURL() {
            const urlParams = new URLSearchParams(window.location.search);
            const lang = urlParams.get('lang');
            if (lang === 'zh' || lang === 'en') {
                return lang;
            }
            return null;
        }
        
        const urlLanguage = getLanguageFromURL();
        const currentLanguage = urlLanguage || 'zh';
        
        $(document).ready(function() {
            updatePageLanguage();
        });
        
        function updatePageLanguage() {
            // 更新所有帶有 data-zh 和 data-en 屬性的元素
            $('[data-zh][data-en]').each(function() {
                const $el = $(this);
                const zhText = $el.data('zh');
                const enText = $el.data('en');
                
                if (currentLanguage === 'zh' && zhText) {
                    $el.text(zhText);
                } else if (currentLanguage === 'en' && enText) {
                    $el.text(enText);
                }
            });
        }
    </script>
</body>
</html>
