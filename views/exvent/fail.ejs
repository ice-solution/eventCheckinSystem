<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>付款失敗</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
<% if (transaction) { %>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card border-danger">
                    <div class="card-header bg-danger text-white">
                        <h4 class="mb-0">付款失敗</h4>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">很抱歉，您的付款未能成功</h5>
                        <p class="card-text">請檢查以下付款細節：</p>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item">訂單編號：<%= transaction && transaction._id ? transaction._id : '' %></li>
                            <li class="list-group-item">票券名稱：<%= transaction && transaction.ticketTitle ? transaction.ticketTitle : '' %></li>
                            <li class="list-group-item">票券價格：$<%= transaction && transaction.ticketPrice ? transaction.ticketPrice : '' %></li>
                            <li class="list-group-item">付款狀態：<%= transaction && transaction.status ? transaction.status : '' %></li>
                            <li class="list-group-item text-danger">錯誤訊息：<%= errorMsg || '' %></li>
                        </ul>
                        <div class="mt-4 text-center">
                            <a href="/web/<%= event_id %>" class="btn btn-secondary">返回主頁</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
<% } else { %>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card border-danger">
                    <div class="card-header bg-danger text-white">
                        <h4 class="mb-0">登記失敗</h4>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">很抱歉，您的登記未能成功</h5>
                        <p class="card-text">請稍後再試，或聯絡主辦單位。</p>
                        <div class="mt-4 text-center">
                            <a href="/web/<%= event_id %>" class="btn btn-secondary">返回主頁</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
<% } %>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html> 